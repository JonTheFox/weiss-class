import {
	//RecoilRoot,
	atom,
	//selector,
	//useRecoilState,
	//useRecoilValue,
} from "recoil";

const quizState = atom({
	key: "quizState",
	default: {},
});

export default quizState;
